{
  "name": "giftbaskets",
  "version": "1.0.0",
  "description": "Gift basket wholesale app (React/Vite + Node/Express + MongoDB) \u2014 No Stripe",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -k -n server,client -c green,cyan \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "npm run start --prefix server",
    "build": "npm run build --prefix client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/example/giftbaskets.git"
  },
  "author": "You",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}